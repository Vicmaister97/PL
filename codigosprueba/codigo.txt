main(){
  initVar
  double j ;
  double w ;
  int i, j, k l;
  char letra;
  boolean verdadero ;
  boolean indefinido;
  boolean no_indefinido;
  int sumador;
  int resultado;
  list_of int lista_ints ;
  list_of int copia_rara;
  list_of int lista_esquinas ;
  turbioo1
  list_of double lista_reales ;
  int valor, posicion;

  endVar

  turbioo2

  lista_ints = [1,2,3] turbioo3;
  copia_rara = lista_ints ++ valor @ @ posicion;
  indefinido = true;
  lista_esquinas = dimeValores(Esquinas(Cuadro(long_aristas, centro), dosDimensiones turbioo4));
  while (indefinido <> no_indefinido) turbioooo{
    ++sumador;
    sumador++;
    turbiooo5
    if(sumador == 3) then turbioo6{
      indefinido = false;
      turbioo5
    }
    turbiooooooo
    else turbioo7{
      letra = "f";
    }
  }
  indefinido = !indefinido;
  resultado=((3+4)*5/7);

  for i := 0 to 10 do{
    out letra;
    out "me llamo";
    out "ç";
    in letra_dada;
    letra = letra_dada;
    j = j-1;
  }
  j = j + w;

  return 0;

}
